/**
 * Appends an element or multiple elements to the end of an array
 * @param {Array} an array of with any kind of elements
 *
 * @returns {number} the new length of the 
 */

function myPushFunction(){}

module.exports = {
    myPushFunction
}